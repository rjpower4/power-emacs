;;; power-rust.el --- -*- lexical-binding: t -*-

;;; Commentary:

;;; Code:
(use-package rustic
  :defer t)
;; (use-package rust-mode                  
  ;; :config
  ;; (setq rust-format-on-save t))

(provide 'power-rust)
;;; power-rust.el ends here
